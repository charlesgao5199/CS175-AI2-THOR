{
  "name": "CS175 AI2-THOR ObjectNav",
  "build": {
    "context": "..",
    "dockerfile": "../docker/Dockerfile"
  },
  "workspaceFolder": "/workspaces/CS175-AI2-THOR",
  "runArgs": [
    "--shm-size=8g"
  ],
  "mounts": [
    "source=${localWorkspaceFolder}/.ai2thor-cache,target=/home/mambauser/.ai2thor,type=bind"
  ],
  "containerEnv": {
    "PYTHONUNBUFFERED": "1"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/opt/conda/envs/ai2thor-objectnav/bin/python",
        "python.terminal.activateEnvironment": true,
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "postCreateCommand": "python --version && python -m pip --version"
}
